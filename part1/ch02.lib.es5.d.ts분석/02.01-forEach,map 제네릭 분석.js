"use strict";
// forEach
[1, 2, 3].forEach((value) => console.log(value));
["1", "2", "3"].forEach((value) => console.log(value));
// map
const strings = [1, 2, 3].map((value) => value.toString());
const numbers = [1, 2, 3].map((v) => v * 1);
